# 在线协作表格

实时多人协作在线电子表格，支持多人同时编辑、单元格合并、中文输入等功能。

## 功能特性

### 基础功能
- 🔄 实时协作 - 多人同时编辑，数据实时同步
- 👥 光标显示 - 实时显示其他用户的光标位置和用户名
- 💾 自动保存 - 数据自动持久化到 SQLite 数据库
- ⌨️ 快捷键支持 - 支持方向键导航、Tab/Enter 切换单元格
- 🎨 用户标识 - 每个用户有独特的颜色标识
- 🌏 中文输入 - 完美支持中文输入法

### 表格功能
- 📝 单元格编辑 - 点击选中后直接输入，双击或按 Enter 进入编辑模式
- 🔗 单元格合并 - 支持选择多个单元格进行合并
- ✂️ 取消合并 - 支持取消已合并的单元格
- 🖱️ 框选支持 - 支持鼠标拖拽选择多个单元格
- ⬆️⬇️⬅️➡️ 方向键导航 - 使用方向键在单元格间移动
- 🗑️ 批量删除 - 支持选中多个单元格后批量清除内容

## 技术栈

- **前端**: Vue 3 + TypeScript + Vite
- **后端**: Node.js + Express + WebSocket
- **协作**: Yjs (CRDT 无冲突复制数据类型)
- **存储**: SQLite (better-sqlite3)

## 快速开始

### 安装依赖

```bash
npm run install:all
```

### 启动开发服务器

```bash
npm run dev
```

### 单独启动

```bash
# 启动后端 (端口 3001)
cd server && npm run dev

# 启动前端 (端口 5173)
cd client && npm run dev
```

## 使用说明

### 基本操作
1. 打开浏览器访问 `http://localhost:5173`
2. 点击单元格进行选择
3. 选中后直接输入文字即可开始编辑
4. 双击或按 Enter 键进入编辑模式
5. 按 Escape 键取消编辑
6. 按 Tab 键切换到下一个单元格

### 协作功能
1. 打开多个浏览器窗口访问同一地址
2. 在一个窗口中编辑，另一个窗口会实时同步显示
3. 可以看到其他用户的光标位置和用户名

### 单元格合并
1. 鼠标拖拽选择多个单元格（或按住 Shift + 点击）
2. 点击工具栏中的"合并单元格"按钮
3. 要取消合并，点击已合并的单元格，再点"取消合并"

### 快捷键
| 按键 | 功能 |
|------|------|
| ↑ ↓ ← → | 方向键导航 |
| Enter | 进入编辑模式 / 确认编辑并向下移动 |
| Tab | 向右移动 |
| Shift + Tab | 向左移动 |
| Delete / Backspace | 清除选中内容 |
| Escape | 取消编辑 |
| Ctrl + A | 全选 |

## 项目结构

```
online-excel/
├── client/              # 前端 Vue 3 项目
│   ├── src/
│   │   ├── components/  # 表格组件
│   │   └── App.vue
│   ├── index.html
│   └── package.json
├── server/              # 后端 Express 项目
│   ├── src/
│   │   ├── index.ts     # 入口文件
│   │   ├── websocket.ts # WebSocket 协作服务
│   │   └── database.ts  # 数据库操作
│   └── package.json
├── package.json
└── README.md
```

## License

MIT
